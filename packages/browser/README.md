# @wai/browser

Chromium-only Playwright wrapper for Step 1 discovery.

## BrowserSession API (Phase 2)

- `start()` / `close()`
- `open(url)`
- `screenshot()`
- `startTrace()` / `stopTrace()`
- `getPage()` / `getContext()` / `getEngine()`

Do not add Firefox/WebKit in Step 1.
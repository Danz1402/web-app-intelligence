# Copy to .env and adjust locally. Never commit .env.

# Matches docker-compose.yml local Postgres
DATABASE_URL=postgresql://wai:wai@localhost:5432/wai

# Artifact roots (relative to repo root)
ARTIFACTS_DIR=artifacts
SCREENSHOTS_DIR=artifacts/screenshots
TRACES_DIR=artifacts/traces